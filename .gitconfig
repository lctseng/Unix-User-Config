[user]
	name = Liang-Chi Tseng
	email = lctseng@cs.nctu.edu.tw
[core]
	editor = vim
	pager = less -R
[alias]
	lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative
        st = status
        s = status -s
        ch = checkout
        co = checkout
        b = branch
        sub = reset HEAD
[color]
	ui = auto
